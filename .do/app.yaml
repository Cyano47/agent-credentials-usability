name: agent-credentials-usability
region: nyc
static_sites:
  - name: web
    git:
      repo_clone_url: https://github.com/Cyano47/agent-credentials-usability.git
      branch: main
    output_dir: dist
    index_document: index.html
    catchall_document: index.html
